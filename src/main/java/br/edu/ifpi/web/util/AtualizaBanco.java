package br.edu.ifpi.web.util;

import java.util.List;

import org.hibernate.Session;
import org.hibernate.criterion.Restrictions;

import br.edu.ifpi.web.dao.DAO;
import br.edu.ifpi.web.modelo.Post;
import br.edu.ifpi.web.modelo.User;




public class AtualizaBanco {
	public static void main(String[] args) {
////		DAO.updateDB();
//		session session = dao.beginsession();
//		list<post> list = session.createcriteria(post.class).add(restrictions.eq("userid",1l)).list();
//		user user = (user) session.createcriteria(user.class).add(restrictions.eq("id",1l)).uniqueresult();
//		user.getposts().addall(list);
//		dao.save(user);
//		session.close();
		
	}
}
